{
  "name": "@cleverbase/frontend-helper",
  "version": "0.2.0",
  "description": "Thin Cleverbase signing frontend helper — redirect orchestration + status polling (no crypto, no secrets).",
  "license": "EUPL-1.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test test/helper.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
