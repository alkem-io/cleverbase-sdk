# Function: resumeHttp()

> **resumeHttp**(`handle`, `status`, `body`, `nowUnix`, `entropy`): `Buffer`

Defined in: [index.d.ts:13](https://github.com/alkem-io/cleverbase-sdk/blob/main/bindings/node/index.d.ts#L13)

Resume after performing an HTTP effect (status + body). Returns a CBOR `{handle, step}` Buffer.

## Parameters

### handle

`Buffer`

### status

`number`

### body

`Buffer`

### nowUnix

`number`

### entropy

`Buffer`

## Returns

`Buffer`
