[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "cleverbase"
version = "0.1.0"
description = "Cleverbase SDK — Python binding"
license = "EUPL-1.2"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
